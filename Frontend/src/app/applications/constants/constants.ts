import {environment} from "../../../environments/environment";

export const constants = {
  GET_COMPANY_LIST : `${environment.gatewayUrl}/api/services/inventory/company`,
  PUT_COMPANY: `${environment.gatewayUrl}/api/services/inventory/company`
}
